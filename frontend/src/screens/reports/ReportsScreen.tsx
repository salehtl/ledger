import { type Scope } from "../../lib/scope";

// v3 scaffold stub — owned and replaced by the reports-networth piece.
export function ReportsScreen(_props: { scope: Scope }) {
  return <div className="text-muted text-sm font-mono">Reports — coming in v3.</div>;
}
