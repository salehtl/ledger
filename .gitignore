# Build artifacts
/ledger
/dist
/web/dist

# Local runtime data (never commit the DB — it holds financial data)
*.db
*.db-wal
*.db-shm
/data/

# Local secrets / env
*.env
ledger.env

# Frontend
frontend/node_modules

# Agent worktree scratch space (never commit)
.claire/

# Brainstorming visual companion (local mockups)
.superpowers/

# Skills installed via `bunx skills add` — machine-local, and .claude/skills/
# entries for them are symlinks into .agents/ that don't survive a clone.
.agents/
skills-lock.json
.claude/skills/shadcn
.claude/skills/migrate-radix-to-base
pid
smoke.pid
node_modules/
